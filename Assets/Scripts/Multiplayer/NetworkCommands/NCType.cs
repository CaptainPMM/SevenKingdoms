namespace Multiplayer {
    namespace NetworkCommands {
        public enum NCType {
            BEGIN_GAME,
            BUILD,
            MOVE_TROOPS,
            RECRUIT,
            SELECT_HOUSE_REQUEST,
            SELECT_HOUSE_RESPONSE,
            SYNC_COMBAT,
            SYNC_COMBAT_END
        }
    }
}